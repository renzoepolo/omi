# Dependencias de desarrollo y CI.
# Van separadas de requirements.txt a proposito: Dockerfile.backend instala solo
# el archivo de produccion y la imagen tiene que seguir liviana.
-r requirements.txt

ruff==0.14.4
pytest-cov==5.0.0
